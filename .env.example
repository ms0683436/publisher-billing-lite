# PostgreSQL
POSTGRES_DB=publisher_billing
POSTGRES_USER=publisher
POSTGRES_PASSWORD=publisher

# Host ports (change these if ports are already in use)
DB_PORT=5432
API_PORT=8000
WEB_PORT=5173

# API -> Postgres (Docker Compose network)
DATABASE_URL=postgresql+asyncpg://publisher:publisher@db:5432/publisher_billing

# Web -> API (optional)
VITE_API_URL=http://localhost:${API_PORT}
